{
  "name": "{{pipeName}}",
  "version": "1.0.0",
  "main": "./bin/main.js",
  "scripts": {
    "build": "pkg bin/main.js --target latest --output dist/{{pipeName}}"
  },
  "bin": {
    "{{pipeName}}": "./bin/main.js"
  },
  "dependencies": {
    "getopts": "^2.3.0",
    "pkg": "^4.4.0"
  }
}
